import { CompletePipePipe } from './complete-pipe.pipe';

describe('CompletePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CompletePipePipe();
    expect(pipe).toBeTruthy();
  });
});
